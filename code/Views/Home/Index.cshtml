<!-- Hero Section -->

<section id="home" class="d-flex flex-column justify-content-center align-items-center text-center min-vh-100 bg-dark">
    @await Html.PartialAsync("~/Views/Sections/Hero.cshtml")
</section>

<!-- Skills Section -->
<section id="skills" class="d-flex flex-column justify-content-center align-items-center text-center min-vh-100 bg-dark py-5">
    @await Html.PartialAsync("~/Views/Sections/Skills.cshtml")
</section>

<!-- Experience Section -->
<section id="experience" class="d-flex flex-column justify-content-center align-items-center text-center min-vh-100 bg-dark py-5">
    @await Html.PartialAsync("~/Views/Sections/Experience.cshtml")  
</section>

<!-- Projects Section -->
<section id="projects" class="d-flex flex-column justify-content-center align-items-center text-center min-vh-100 bg-dark py-5">
    @await Html.PartialAsync("~/Views/Sections/Projects.cshtml")
</section>
